// Export image paths for static assets
// Add image imports here when you have assets
// Example: export const logo = new URL('./logo.png', import.meta.url).href;

export const images = {} as const;
